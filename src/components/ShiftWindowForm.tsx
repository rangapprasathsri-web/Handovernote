import React from 'react';
import { ShiftConfigurationForm, ShiftConfigurationFormProps } from './ShiftConfigurationForm.js';

export const ShiftWindowForm: React.FC<ShiftConfigurationFormProps> = (props) => {
  return <ShiftConfigurationForm {...props} />;
};
